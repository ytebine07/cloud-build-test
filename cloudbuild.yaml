steps:
  # imageのBuild
  - name: "gcr.io/cloud-builders/docker"
    args:
      ["build", "-t", "gcr.io/devtest-cloudrund-handson/my-first-image", "."]
    timeout: 500s

  # cloud registryにPush
  - name: "gcr.io/cloud-builders/docker"
    args:
      ["build", "-t", "gcr.io/devtest-cloudrund-handson/my-first-image", "."]
